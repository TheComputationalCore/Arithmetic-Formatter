
Paste:

```markdown
# Usage Guide

This guide explains how to use **Arithmetic Formatter** via the Python API and the CLI.

---

## 🧪 Python API

```python
from arithmetic_formatter import arithmetic_arranger

result = arithmetic_arranger(["32 + 698", "1 - 2"], show_answers=True)
print(result)
